<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sunvua.ljy.dao.ItemDOMapper">
    <resultMap id="BaseResultMap" type="com.sunvua.ljy.daoObject.ItemDO">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="price" jdbcType="DOUBLE" property="price" />
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="sales" jdbcType="INTEGER" property="sales"/>
        <result column="img_url" jdbcType="VARCHAR" property="imgUrl"/>
    </resultMap>
    <sql id="Base_Column_List">
        id,title,price,description,sales,img_url
    </sql>
    <insert id="insert" parameterType="com.sunvua.ljy.daoObject.ItemDO" useGeneratedKeys="true" keyProperty="id">

        insert into item
        (id,title,price,description,sales,img_url)
        values (#{id},#{title},#{price},#{description},#{sales},#{imgUrl})
    </insert>
    <select id="getItemById" parameterType="INTEGER" resultMap="BaseResultMap">
        select id,title,price,description,sales,img_url
        from item
        where id=#{id}
    </select>
    <update id="increaseStock">
        update item
        set sales=sales+#{amount}
        where id=#{id}
    </update>
</mapper>